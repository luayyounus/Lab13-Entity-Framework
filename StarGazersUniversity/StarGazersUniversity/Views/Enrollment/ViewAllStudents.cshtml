@model List<StarGazersUniversity.Models.Student>

@if (!Model.Any())
{
    <p>We don't have any students enrolled.</p>
}
else
{
    <h2> Students </h2>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Birth Date</th>
                <th>City</th>
                <th>Class</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var student in Model)
            {
                <tr>
                    <td>@student.FirstName</td>
                    <td>@student.LastName</td>
                    <td>@student.DOB</td>
                    <td>@student.City</td>
                    <td>@student.Class</td>
                </tr>
            }
        </tbody>
    </table>
}